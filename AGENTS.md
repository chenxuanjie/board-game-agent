# 工程设计要则

适用于本 Flutter 工程的产品、UI 与实现约束。

## 核心方向

- 程序设计保持简洁，优先解决真实需求，避免无必要的抽象、配置和流程。
- UI 保持简约、高级、有设计感；让层级、留白和排版承担主要表达，不依赖堆叠装饰。
- 新功能先确认用户任务和主要操作，再决定页面元素；次要信息不得抢占主操作。

## 文案与信息密度

- 标题、按钮和状态文案短而明确，使用用户能直接理解的动作词。
- 删除重复的说明性文字、口号和无实际帮助的提示；不要在每张卡片中重复解释同一件事。
- 仅在用户需要决策、等待或处理错误时显示必要说明；帮助信息优先使用简短副文案、Tooltip 或按需展开。
- 空状态、加载状态和错误状态必须清楚，但保持一到两句话，并提供下一步操作。

## UI 视觉与交互

- 优先复用项目已有的主题令牌、间距、圆角、阴影、图标和组件，不在页面内随意创造新样式。
- 保持稳定的视觉节奏：统一对齐、间距、字号层级和控件尺寸；避免元素大小不一、贴边或无意义的宽幅留白。
- V4 桌面首页与窗口的尺寸基线见 [V4 桌面首页尺寸约束](v4-ui-size-constraints.md)；调整相关布局时同步更新该文档和回归测试。
- 每个页面突出一个主要操作，次要操作降级为文本按钮、图标按钮或菜单。
- 颜色和装饰服务于层级与状态；避免过度渐变、过多颜色、重复边框和装饰性文字。
- 所有可交互控件都要有明确的悬停、聚焦、禁用、加载、成功和失败反馈，并保持键盘与触控可用。

## 实现与验证

- 先复用现有组件和状态边界，再新增代码；避免用页面级临时补丁掩盖共享设计问题。
- 异步操作必须区分空闲、加载中、成功、空结果和失败，失败状态保留可重试入口。
- 桌面、Web 和 Android 都按目标尺寸验证，不以单一平台或单一窗口大小推断正确性。
- 提交前至少检查：文案是否精简、主次操作是否清晰、窄窗口与最大化布局是否稳定、关键按钮是否真的生效。

## AI 框架与规范参考

本项目的 AI 对话、Agent Run、流式事件、工具调用、知识库和引用功能，必须优先参考成熟的官方规范和开源实现。参考不是只模仿截图或静态文案，而是检查其完整的数据流、状态机、失败恢复、持久化、可观测性和测试方式。

### 已确认的参考来源

- OpenAI Responses API：流式事件、`output_text.delta`、`output_text.done`、`response.completed`、工具调用、引用和不完整响应的协议基线。
- OpenAI Agents SDK：Agent、工具、handoff、session、trace、run 生命周期和事件语义的官方参考。它主要提供 Python/TypeScript 实现，不得假设存在可直接用于 Flutter 的官方 Dart 版本。
- OpenHands：最接近 Codex 的 Agent Run 参考，重点关注 Plan、reasoning summary、工具轨迹、暂停/恢复和长流程状态。
- LibreChat：多模型、多服务商、会话、Web Search、引用、流式传输和断线恢复的完整聊天产品参考。
- Open WebUI：知识库、RAG、来源、引用、工作流状态和 OpenAI 兼容服务的产品参考。
- Vercel AI SDK：流式协议、文本块/工具块分离、结构化输出和工具状态的 TypeScript 参考；不能直接当作 Flutter 依赖。
- LobeHub：工作区、项目上下文、Agent 隔离、长期记忆和多 Agent 交互参考。
- Dify：完整 AI 应用、知识库、模型供应商和工作流平台参考；接入前必须审查其开源许可证和商用条款。
- RAGFlow：PDF/文档解析、切片、检索、引用和知识库管理参考，适合作为桌游规则资料的 RAG 产品参考。
- Haystack：可组合、可测试、面向生产的 RAG/Agent pipeline 参考。
- LlamaIndex：数据连接器、索引、检索、结构化提取和 Agent 集成参考。
- LangChain/LangGraph：工具编排、状态图、checkpoint、暂停恢复和长流程 Agent 参考，不把它们误认为单纯的向量检索库。
- LiteLLM：多服务商路由、重试、fallback、预算、限流和 OpenAI 兼容网关参考。
- FastGPT：中文知识库、工作流和引用交互参考；接入前必须审查其许可证和商用条款。
- `openai_dart`：当前 Flutter/Dart 工程使用的 API 客户端参考。它只负责 API 调用和数据映射，不等同于 Agents SDK、RAG 引擎或聊天产品。

### 新功能的复用优先级

开发或修改任何 AI 功能前，必须按以下顺序执行：

1. 明确功能属于协议适配、Agent 编排、RAG、服务商路由、UI 交互还是持久化，并搜索上述项目对应的源码、测试、文档和发布版本。
2. 优先采用官方 SDK、成熟开源库或可独立部署的服务，而不是在本项目中重新实现同一能力。能直接引入时，要记录项目、版本、源码路径、依赖、许可证、平台限制和升级策略。
3. 只有在语言、运行时、平台、许可证、安全边界或架构不兼容时，才改为参考其思想；必须在改动说明中写明不能直接复用的具体原因，不得以“参考过”代替实现。
4. 直接复用代码必须保留许可证和版权声明，进行安全审查、依赖审查和 API 兼容性验证，不复制品牌、页面文案或与本项目无关的实现。
5. 对于同一能力，禁止同时保留一套自研实现和一套未使用的开源实现。确定采用的方案后，删除重复的临时代码和旧状态路径。

### 完整链路要求

AI 功能只有在完整链路完成并通过验收后才能标记为完成，不得只交付 demo、静态卡片或单一成功路径。至少要覆盖：

- 请求创建、上下文选择、权限校验、模型/服务商能力检查和幂等键；
- 流式事件接收、顺序校验、去重、背压、`delta` 批量刷新、`done`/`completed` 收尾和原始事件保留；
- Agent 阶段、工具调用、引用、最终答案的分层模型和明确状态转换；
- 超时、断网、服务端 4xx/5xx、鉴权失败、限流、模型不可用、协议解析失败、取消和重连；
- 有界指数退避、最大重试次数、可取消重试、断线恢复和已完成请求去重；
- Session、上下文、Compaction、Run 轨迹、滚动/展开状态的持久化和版本隔离；
- 用户可理解的错误信息、部分答案边界、引用真实性和敏感内部字段过滤；失败时保留 `_failureNotice` 的失败原因、尝试次数和部分答案，并将每个已确认阶段的真实资料/引用进度完整展示；
- 桌面、Web、Android 的事件与状态一致性，以及窄窗口、长文本和无网络环境；
- 单元测试、协议顺序测试、故障注入、集成测试、截图/golden 测试、长对话和压力测试；
- `runId`、`responseId`、阶段、模型、请求次数、耗时、token usage、引用数和错误码等可观测性字段。

### 企业级模仿门槛

- “参考某项目”必须落实到可追溯的源码/文档链接、版本和具体行为，不能只写在说明中。
- 任何中间状态、搜索原文、工具参数和协议字段都必须经过分层和脱敏后再展示给用户。
- 所有外部依赖必须有许可证、维护活跃度、漏洞和升级风险记录；高星不等于可以直接商用。
- 所有异步路径必须有超时、取消和失败收敛逻辑；不得让旧 Run 在会话切换后继续写入当前 UI。
- 交付前必须提供验收用例、已知限制、三端验证结果和回滚方案。未完成的边界必须明确标记为未完成，不得用“后续再补”掩盖半成品。
