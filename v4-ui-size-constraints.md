# V4 桌面首页尺寸约束

本文记录 V4 Windows 桌面端首页在标准窗口下的尺寸基线，供后续布局调整与回归检查使用。

- 下列数值均为 Flutter 逻辑像素（dp）；Windows 实际屏幕像素会受系统显示缩放影响。
- 标准参考窗口为居中的 `1280 × 800`。低于该基准时保持原有紧凑/抽屉重排；高于该基准时使用同一设计画布按统一比例扩展，比例范围为 `1.0–1.35`。
- 调整相关布局时，应同步更新本文及对应的回归测试。

## 尺寸基线

| 区域 | 默认尺寸与布局 | 实现依据 |
| --- | --- | --- |
| 启动窗口 | `1280 × 800`，居中、普通窗口启动；最小尺寸 `1100 × 800`。最小高度与默认高度一致，使完整侧栏的导航和底部插图保持独立区域；Web 低于该尺寸时不进入宽屏桌面壳。 | `lib/main.dart`；`windows/runner/main.cpp`；`lib/ui/desktop/desktop_responsive.dart` |
| 窗口控制与拖动 | 隐藏系统标题栏，使用应用自绘最小化、最大化/还原、关闭按钮；每个 `32 × 20`，合计 `96 × 20`，贴在右上角。顶部 `18` dp 高的区域用于拖动/双击最大化；窗口边缘 `6` dp 用于调整大小。按钮与拖动区叠加在应用顶部，不额外增加窗口高度。 | `lib/ui/desktop/window_controls.dart`；`lib/ui/desktop/workspace.dart` |
| 应用顶部工具栏 | 基准高 `92`；内边距左/右 `20 / 18`、上 `24`、下 `14`。搜索框基准高 `50`、基准最大宽 `780`；头像基准 `44 × 44`。宽屏设计画布同步放大，窗口控制区保留原生操作热区。 | `lib/ui/desktop/workspace.dart`；`lib/ui/desktop/desktop_responsive.dart` |
| 左侧栏 | 桌面基准宽 `205`、占满窗口高度；顶部留白 `24`；Logo 盒子 `77 × 72`。完整侧栏和 `76` dp 图标栏共用高 `152` 的头部占位，使导航首项及后续图标在切换前后保持相同纵坐标；导航项高 `44`、项间距 `3`，图标盒统一为 `28 × 28`，横向中心保持一致，与文字间距 `10`。宽屏设计画布下随 Metrics 同步放大。底部插图使用原图比例 `971:1619`，固定贴住侧栏左、右、下边并按 `BoxFit.fitWidth` 完整显示；在 `800` 高窗口中，插图顶部位于最后一个导航项下方，不与“设置”重叠；图标栏及收起状态不显示插图。窗口内容宽度 `<1200` 时切换为图标栏；宽度 `<760` 时改为抽屉导航，抽屉宽 `280`。 | `lib/ui/desktop/sidebar.dart`；`lib/ui/desktop/workspace.dart` |
| 首页主体 | 在 `1280` 窗口宽度、`205` 侧栏下，工作区宽 `1075`。主体左右内边距分别为 `15 / 12`，可用宽约 `1048`；右栏宽 `282`、两栏间距 `14`，左侧主栏约 `752`。宽于基准窗口时，设计画布和两栏尺寸按统一比例同步扩展。 | `lib/ui/desktop/workspace.dart`；`lib/ui/desktop/home_pane.dart`；`lib/ui/desktop/desktop_responsive.dart` |
| 首页横幅 | 横幅位于左主栏，参考宽约 `752`。统一外框按第二张横幅素材比例 `2169:725`，高度约 `251`；圆角 `12`。第一张素材带内嵌侧边留白，使用 `1.04` 倍中心缩放裁齐。截图中的旧比例 `782:256`（约 `752 × 246`）已由统一第二张横幅外框的比例替代。 | `lib/ui/v4/v4_home.dart:171–173,214–230` |
| 首页游戏卡片 | 默认展示一行最多 `5` 张；卡片基准为 `140 × 229`，封面区域高 `116`，卡片水平/行间距 `13`。在宽屏设计画布中卡片与文字按 Metrics 同步放大；空间不足时仍保留横向滚动，不强行压缩内容。 | `lib/ui/desktop/home_pane.dart`；`lib/ui/desktop/desktop_responsive.dart` |
| 首页最近浏览 | 面板高 `171`，最多显示最近 `3` 条记录；条目使用固定三槽位布局，单条最大宽 `126`、间距 `8`，不足三条时不拉伸。缩略图比例为 `16:9`，优先显示横幅素材，并显示相对浏览时间。 | `lib/ui/desktop/home_pane.dart` |
| 收藏状态按钮 | 详情页“喜欢/已喜欢”按钮固定宽 `108`、高 `48`。状态切换只替换内部图标、文案和配色，并使用淡入淡出过渡，不改变外框尺寸。 | `lib/ui/desktop/game_detail_pane.dart` |
| 桌游库工具栏 | 分类、排序和筛选按钮统一高 `38`、圆角 `20`，字体统一为 `13`、行高 `1`，激活态字重 `700`、未激活态 `500`；使用同一边框、悬停底色和橙色选中渐变。分类区靠左，排序/筛选组成紧凑操作组靠右；窄于 `720` 时操作组换至第二行右对齐。弹层和箭头使用 `180ms` 进入、`150ms` 退出过渡。 | `lib/ui/desktop/games_pane.dart` |
| 右侧首页卡片 | 右栏基准宽 `282`；个人中心高 `255`，统计卡片图标盒为 `52 × 52`；聚会面板高 `157`、快捷入口高 `169`、底部横幅高 `104`；区块之间垂直间距 `12`。宽屏设计画布中这些尺寸按 Metrics 同步放大。 | `lib/ui/desktop/home_pane.dart` |

## 响应式规则

| 工作区宽度 | 导航形态 | 说明 |
| --- | --- | --- |
| `≥1200` | `205` dp 完整侧栏 | 标准桌面导航。 |
| `760–1199` | `76` dp 图标栏 | 保留导航图标，收起文字。 |
| `<760` | 抽屉导航 | 侧栏从主布局中移除，通过导航按钮打开 `280` dp 抽屉。 |

主页左右栏在窄窗口下由首页自身重排，公共双栏组件不再自行猜测窄屏断点。当前统一规则如下：

| 内容区域 | 响应式规则 |
| --- | --- |
| 桌游库海报 | 使用 `Wrap` 自适应列数：内容宽度 `≥1130` 为 5 列，`860–1129` 为 4 列，`640–859` 为 3 列，更窄时为 2 列；水平间距 `20`、行间距 `24`，卡片比例约 `1:1.49`。 |
| 桌游库筛选与排序 | “综合排序”和“筛选”均从按钮下方锚定打开悬浮菜单；排序菜单提供综合排序、名称、评分，筛选菜单提供人数、时长、类型、难度和评分，并通过“应用筛选”提交条件。 |
| 设置卡片 | 内容区 `<700` 为 1 列，`700–1099` 为 2 列，`≥1100` 为 3 列；卡片间距 `13`。 |
| 首页双栏 | 内容区 `≥900` 使用主栏 + `282` 宽右栏；更窄时由首页决定上下排列。 |
| 超宽窗口 | 首页、桌游库/喜欢、设置页保留基准最大宽度 `1480 / 1500 / 1400`，并随统一 Metrics 比例扩展；设计画布自身最大放大比例为 `1.35`，超出后居中保留留白。首页横幅不再使用独立的 `900` 宽上限，与主栏对齐。 |
| 详情页英雄区 | 宽度 `<760 / 760–1099 / ≥1100` 时高度为 `320 / 380 / 450`；详情介绍内容在窄宽度下改为上下排列。 |

实现集中在 `lib/ui/desktop/desktop_responsive.dart`、`lib/ui/desktop/workspace.dart` 与各桌面页面，相关回归断点测试位于 `test/desktop_responsive_test.dart` 和 `test/desktop_workspace_test.dart`；表中的 `752 / 282` 等主体尺寸仍只作为标准参考窗口下的基准。
