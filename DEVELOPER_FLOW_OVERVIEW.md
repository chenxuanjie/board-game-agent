# 启动与运行流程概览

这份文档面向开发者，描述当前 App 的启动流程、资源更新逻辑和主要使用链路。目标是帮助快速理解系统怎么跑起来、什么时候会访问远端、什么时候会走本地缓存。

## 1. 启动流程

整体入口在 `lib/main.dart`。启动后会先创建 `AppController`，然后由页面层等待 `controller.initialize()` 完成。

初始化阶段主要做这些事：

1. 读取本地偏好设置  
   包括语言、配色、语音朗读开关、AI 回答模式、AI 接口配置、资源源配置等。

2. 加载桌游目录、展示配置和资源 manifest
   当前实现优先使用本地缓存，其次使用包内的 `catalog.json`、`game.json` 和 `manifest.json`，不在首屏初始化阶段阻塞式拉远端资源。
   这样首页能先起来，避免因为某个资源源响应慢导致一直卡在启动页。

3. 初始化语音相关能力  
   包括语音输入能力检测和 TTS 初始化。失败时不会阻塞整体启动，只是相关功能不可用。

4. 构建初始会话状态  
   如果当前没有聊天记录，会插入一条默认欢迎语。

5. 异步触发资源预加载和远端更新检查  
   这两部分不阻塞首页显示，进入首页后后台继续执行。

## 2. 首页加载后的后台工作

首页出来之后，系统会继续做两类后台任务。

### 2.1 首页资源预加载

`prefetchHomeImages()` 会把首页会用到的封面图、背景图、画廊图提前缓存到本地。  
现在首页会显示一个“正在加载资源...”的提示条，用于告诉用户图片还在补齐中。

资源缓存逻辑统一走 `RemoteAssetService.ensureCached()`：

- 本地已有缓存时直接复用
- 本地没有时按资源源顺序尝试下载
- 下载成功后写入应用缓存目录

### 2.2 远端资源更新检查

`checkForLibraryUpdates()` 会在后台检查远端内容是否有更新。逻辑是：

1. 先对比远端 `assets/catalog.json` 和本地 catalog
2. 如果 catalog 有变化，直接认为有一批桌游元数据需要更新
3. 如果 catalog 没变，再逐个检查已跟踪的远端资源路径  
   包括封面、背景、manifest 登记的所有资源、`game.json` 和 `manifest.json`

更新比较主要依赖远端 `HEAD`/响应头信息，比如：

- `etag`
- `last-modified`
- `content-length`

如果发现有变化，会生成一个待更新列表，首页弹窗提示用户是否执行本地更新。

## 3. 用户确认更新后的逻辑

当用户同意更新时，`applyPendingLibraryUpdate()` 会执行这些动作：

1. 对待更新的远端路径逐个重新下载并覆盖本地缓存
2. 清空已解析资源路径缓存
3. 重新加载桌游列表
4. 再跑一轮首页图片预加载

这里的设计目标是：  
**运行时优先读本地缓存，远端变化只在明确检查和确认更新后再落地到本地。**

## 4. 页面层使用逻辑

### 4.1 首页

首页展示桌游列表、AI 入口、资源连通状态和更新提示。  
卡片图片本身也是懒解析的：页面先渲染占位，图片到本地后再替换成真实文件。

### 4.2 桌游详情页

详情页会展示：

- 背景图 / 封面图 / 图集
- 桌游基础信息
- 规则书入口
- FAQ 入口
- AI 助手入口

规则书和 FAQ 打开时，先尝试把对应远端文档缓存到本地，再根据类型进入：

- Markdown 文档页
- PDF 文档页

### 4.3 通用 AI 与桌游 AI

当前有两个 AI 入口：

1. 桌游详情里的 AI 助手  
   绑定当前桌游上下文

2. 首页的通用 AI 入口  
   仍然带当前选中桌游上下文，但入口语义更偏“独立 AI”

两者共用同一个 `ChatScreen`，区别主要是 `useGlobalMode` 开关和对应文案/偏好设置。

## 5. AI 回答链路

AI 核心服务是 `ResponsesRulesWorkflow`。当前回答模式分两种：

- `knowledgeOnly`：只按本地知识库回答，不够就返回“不知道”
- `knowledgeThenDirect`：先按知识库回答，不够再结合当前桌游资料直接回答

一次提问的大体流程是：

1. `AppController.sendPrompt()` 收到用户输入
2. 读取当前语言、桌游、回答模式、AI 配置
3. `ResponsesRulesWorkflow` 先按当前桌游 manifest 选择知识资源和文档
4. 根据回答模式决定：
   - 只做知识库回答
   - 或先知识库回答，再做直接补充
5. 回答完成后写入消息列表
6. 如果开启语音朗读，再交给 TTS 播报

## 6. 资源系统的基本原则

当前资源系统可以简单理解为三层：

1. 包内资源  
   用于首包可用、离线兜底；各桌游的 manifest 和本地知识 Markdown 会随应用内置

2. 本地缓存  
   用于运行时快速读取，尽量避免重复访问远端

3. 远端资源库  
   用于更新 catalog、图片、文档、各桌游 `game.json` 和 `manifest.json`

运行时大多数读取逻辑遵循：

- 能用缓存就先用缓存
- 缓存没有时再尝试远端
- 远端失败时尽量回退到包内资源

## 7. 当前设计重点

目前这套流程的几个关键点是：

- 首屏启动优先保证“先进入首页”，不让远端请求阻塞启动
- 远端更新检查放到后台异步进行
- 文档、图片、知识库、manifest 都尽量走统一缓存逻辑
- AI 回答优先利用本地知识库，必要时再做智能补充

如果后续要继续演进，最值得优先关注的地方通常是：

- 远端资源更新策略是否要更细粒度
- manifest / 文档 / 图片的并发与超时策略
- 首页资源加载提示是否需要更精细的用户反馈
- AI 模式切换和知识库命中率的可观测性
